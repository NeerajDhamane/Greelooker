// Single source of truth for the brand name.
// Import this anywhere the brand name needs to be displayed instead of
// hardcoding the raw string (see Bug #18 in the master reference doc).
export const BRAND_NAME = 'GreeLooker'
